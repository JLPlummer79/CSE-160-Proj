
// Goal of Flood: Broadcast a packet to every node on the network JLP
#include "../../includes/packet.h"

interface Flood {
    //just a stub for now JLP
    //maybe flood needs to be moved here, and rely on ping with different paramaters...
    //command error_t flood(pack msg, uint16_t dest)
    command void pass(); //dummy
    
}